{
  "scripts": {
    "dev": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "deploy": "npm run build && ./deploy.sh",
    "deploy-ec2": "echo 'EC2デプロイ用のコマンドです。deploy.shを実行してください。'"
  }
}
