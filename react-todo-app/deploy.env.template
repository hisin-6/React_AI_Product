# デプロイ用環境変数設定ファイル（テンプレート）
# 実際の値を設定してdeploy.envとして保存してください

# サーバーIPアドレス
export SERVER_IP="your-server-ip-here"

# Webサーバーのドキュメントルート（通常は以下のいずれか）
# Ubuntu/Debian: /var/www/html
# CentOS/RHEL: /usr/share/nginx/html
export WEBROOT_PATH="/usr/share/nginx/html"

# Webサーバーのユーザー（通常は以下のいずれか）
# Ubuntu/Debian: www-data
# CentOS/RHEL: nginx
export WEB_USER="nginx"

# 使用方法:
# 1. このファイルをdeploy.envとしてコピー
# 2. 実際の値を設定
# 3. source deploy.env
# 4. ./deploy.sh
